From the Command line you can now just run

gradle bootRun

or you can run it from the compiled jar file

./gradlew build && java -jar build/libs/lego-0.0.1-SNAPSHOT.jar
Note: this is considered a production environment, and dev tools will be disabled.
